# config_prod.py

# Configuration for the production environment
PORT = 3000  # Port for the production environment
DB_PATH = "production_management.db"  # Path to the production SQLite database
DEBUG = False  # Disable debugging in production
